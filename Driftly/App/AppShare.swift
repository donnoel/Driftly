import Foundation

enum AppShare {
    /// Public App Store URL to share; replace placeholder with the real link.
    static let appStoreURL = URL(string: "https://apps.apple.com/app/idYOUR_APP_ID")!
}
